---
mode: 'agent'
description: 'Change branch name'
---

Execute the following Git workflow:
1. Create a new branch the name `feature/update-branch-name` using the command: `git checkout -b feature/update-branch-name main`.
2. Switch to the new branch.