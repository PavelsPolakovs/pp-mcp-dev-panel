---
mode: 'agent'
description: 'Push changes'
---

Execute the following Git workflow:
1. Push the current branch to the remote repository using the command: `git push origin <branch-name>`, replacing `<branch-name>` with the name of your current branch.
